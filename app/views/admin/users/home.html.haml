%table{:class => "table"}
  %thead
    %tr
      %th Username
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.username
        %td= link_to 'Show', user, :class => "btn btn-success"

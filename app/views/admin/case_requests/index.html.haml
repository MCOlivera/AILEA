%h1 Case Requests

%table{:class => "table"}
  %thead
    %tr
      %th User
      %th Case Title
      %th Action

  %tbody
    - @case_requests.each do |case_request|
      %tr
        %td= case_request.user.username
        %td= case_request.title
        %td
          = link_to 'Done', edit_case_request_path(case_request), :class => "btn btn-success"
          = link_to 'Destroy', case_request, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => "btn btn-danger"

